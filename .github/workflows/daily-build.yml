name: Daily Build
on:
  schedule:
  - cron: '0 0 * * *'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Trigger our build webhook on Netlify
      run: curl -s -X POST "${BUILDHOOK}"
      env:
        BUILDHOOK: ${{ secrets.NETLIFY_BUILD_HOOK }}
        
